<h1><%= @article.title %></h1>

<p><%= @article.body %></p>

<ul>
  <li><%= link_to "Edit Course", edit_article_path(@article) %></li>
  <li><%= link_to "Delete Course", article_path(@article),
                  method: :delete,
                  data: { confirm: "Are you sure?" } %></li>
</ul>

<h2>Enrolled Students</h2>
<%= render @article.comments %>

<h2>Enroll a Student:</h2>
<%= render 'comments/form' %>
